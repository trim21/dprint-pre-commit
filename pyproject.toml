[build-system]
requires = ["flit-core~=4.0"]
build-backend = "flit_core.buildapi"

[project]
name = "dummy-package"
version = "0"
description = "mirror for meson"
authors = []
license = { text = 'MIT' }

requires-python = "~=3.8"

dependencies = [
  'dprint-py==0.56.1.0',
]
